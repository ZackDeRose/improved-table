import { SuperlativePipe } from './superlative.pipe';

describe('SuperlativePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperlativePipe();
    expect(pipe).toBeTruthy();
  });
});
